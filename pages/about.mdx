import Layout from '../components/Layout';

export default ({ children }) => (
  <Layout description="About" title="About" hasMarkdown={true}>
    {children}
  </Layout>
);

# About

This is the about page

Look, a button! 👇
