import { Layout } from '../core/components';

export default ({ children }) => (
  <Layout description="About" title="About">
    {children}
  </Layout>
);

# About

This is the about page

Look, a button! 👇
